# Benchmark artifacts

- `work/` — generated JSONL, bincode cache, and output files (gitignored). Created by the harness or binaries.
- `results/` — run manifests and summaries (gitignored).

Use the repo root harness: `./harness/run.sh --help`.
